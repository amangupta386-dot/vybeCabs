# Set to development to return real error messages from 500 responses (never use in production).
# NODE_ENV=development

PORT=3000
DATABASE_URL=postgres://postgres:postgres@localhost:55432/driver_matching
REDIS_URL=redis://localhost:6379
MATCH_RADIUS_KM=10
# Optional: when set, POST /drivers and POST /rides* require Authorization: Bearer <API_KEY>
# API_KEY=
